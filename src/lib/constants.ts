import { ScoreWeights } from '../types';

export const DEFAULT_WEIGHTS: ScoreWeights = {
  presence:   10,
  lesson:     10,
  pg:         10,
  bibleStudy: 15,
  mission:    20,
  visit:       5,
};
